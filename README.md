# Savior Lab Notes

Astro 5.x 기반 블로그 — Vibe Coding, Local LLM case study.

```bash
npm i && npm run dev   # localhost:4321
```

Content: `src/content/cases/*.md` | Pages: `/`, `/case`, `/case/[slug]`, `/editor`
